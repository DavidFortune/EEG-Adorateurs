[project]
name = "token-generator"
version = "0.1.0"
description = "FastAPI service for generating Stream Chat tokens"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "python-dotenv>=1.2.1",
    "stream-chat>=4.28.0",
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.34.0",
]
